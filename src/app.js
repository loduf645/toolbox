/* ============================================================
   INIT — bootstrap aplikasi (WAJIB file terakhir dalam bundel)
   ============================================================ */
initSearch();
render();
